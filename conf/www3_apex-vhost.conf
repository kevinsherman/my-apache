<VirtualHost *:80>

    ServerName wtpublish
    ServerAlias ${PUBLISH_HOSTNAME_WTU3} ${PUBLISH_HOSTNAME_WTU_APEX}

    # 301 Request as-is to www.wtu.local
    # RewriteRule ^(.*)$ http://${PUBLISH_HOSTNAME_WTU}$1 [R=301,QSA,L]

</VirtualHost>
